# Server-side environment variables (Vercel)
# DO NOT commit .env.local - this is just an example

# Gemini API Key (server-side only, never exposed to client)
GEMINI_API_KEY=your_gemini_api_key_here
